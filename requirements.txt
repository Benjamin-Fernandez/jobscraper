# JobScraper runtime dependencies. Install with:  pip install -r requirements.txt
# Lanes append what they introduce (PRD section 0.6); never edit others' lines.

PyYAML>=6.0          # config, watchlist, rules
httpx>=0.27          # net.py - every scrape request
openpyxl>=3.1        # v1 legacy (ingest.py, output.py); goes in M9-T1

# D-1: resume ingest (M2)
pypdf>=4.0
python-docx>=1.1

# D-3: web app (M6, M7)
fastapi>=0.110
uvicorn>=0.29

# Optional: only for budget.backend: api, which needs ANTHROPIC_API_KEY.
# The default backend (cli) uses the Claude Code CLI and needs no package.
# anthropic>=0.40
