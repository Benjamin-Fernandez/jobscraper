"""Fetching postings from careers sites.

Carried over from v1 essentially unchanged - the ATS adapters and the discovery
that chooses between them are the most valuable code in this repo. Only their
input type changes: a watchlist entry instead of a spreadsheet row.
"""
