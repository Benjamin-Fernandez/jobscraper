"""The single-page application: one page, one API, no generated HTML files.

v1 wrote a new HTML file per run, so reviewing results meant hunting through a
folder. This serves one page that changes which run it is showing.

May import `store`, `shortlist`, `config` and `models` - never a pipeline stage.
"""
