<script setup>
// Placeholder until M12-T4: companies per run.
import { tabEmits, tabProps } from '../shell.js'

defineProps(tabProps)
defineEmits(tabEmits)
</script>

<template>
  <section class="settings empty">
    <p class="empty-title">Settings</p>
    <p class="muted">Setting companies per run from here is coming next.</p>
  </section>
</template>
